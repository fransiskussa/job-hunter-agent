name: Job Hunter Agent

on:
  schedule:
    # 18:02 WIB (11:02 UTC) untuk testing
    - cron: '0 * * * *'
  workflow_dispatch: # Memungkinkan trigger manual

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'
        cache-dependency-path: scraper_project/requirements.txt

    - name: Install Dependencies
      working-directory: scraper_project
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Cache Playwright Browsers
      id: playwright-cache
      uses: actions/cache@v4
      with:
        path: ~/.cache/ms-playwright
        key: ${{ runner.os }}-playwright-${{ hashFiles('scraper_project/requirements.txt') }}

    - name: Install Playwright Browsers with Deps
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      working-directory: scraper_project
      run: |
        playwright install chromium --with-deps

    - name: Install Playwright System Deps Only
      if: steps.playwright-cache.outputs.cache-hit == 'true'
      working-directory: scraper_project
      run: |
        playwright install-deps chromium
        
    - name: Install xvfb (Virtual Display)
      run: sudo apt-get install -y xvfb

    - name: Run Job Hunter Pipeline
      working-directory: scraper_project
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
        GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
        PYTHONPATH: .
      run: |
        xvfb-run -a python src/workflows/run_pipeline.py
