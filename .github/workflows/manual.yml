name: Manual workflow

on:
  push:
    branches:
     -main
  schedule:
    - cron: '* * * * *'

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Send greeting
        run: |
          echo "Hello ${{ github.event.inputs.name || 'World' }}"
